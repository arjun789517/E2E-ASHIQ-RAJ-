# R4J M1SHR4 - Facebook Message Automation (Flask)

## Features
- User signup/login (admin user: ASHIQRAJ / ASHIQRAJ)
- Configure chat ID, messages, delay, name prefix, cookies
- Start/stop headless automation using Selenium
- Live console output (polling every 2 seconds)
- Dark theme with light blue borders
- SQLite database (persistent)

## Run Locally
```bash
pip install -r requirements.txt
python app.py
